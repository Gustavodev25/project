# =======================
# API Configuration
# =======================
# Backend URL para chamadas server-side (usado pelo middleware Next.js)
# Se configurado, todas as rotas /api/meli/* e /api/shopee/* farão proxy para este backend
# Deixe vazio para usar Prisma local (modo standalone)
BACKEND_URL=

# ❌ DEPRECATED: Não usar NEXT_PUBLIC_API_URL (expõe no browser e causa erro 401)
# NEXT_PUBLIC_API_URL=
# NEXT_PUBLIC_FORCE_EXTERNAL_BACKEND=false

# =======================
# Database
# =======================
DATABASE_URL="postgresql://user:password@host/database"

# =======================
# Authentication
# =======================
# Segredo compartilhado para sess��es (use o mesmo em frontend e backend)
AUTH_SECRET=
# Fallback para JWT (caso n��o defina AUTH_SECRET, usar�� CRON_SECRET ou JWT_SECRET)
JWT_SECRET=

# =======================
# Mercado Livre
# =======================
MELI_APP_ID=
MELI_CLIENT_SECRET=
MELI_REDIRECT_URI=
MELI_REDIRECT_ORIGIN=
MELI_AUTH_BASE="https://auth.mercadolibre.com"
MELI_API_BASE="https://api.mercadolibre.com"

# =======================
# Shopee
# =======================
SHOPEE_PARTNER_ID=
SHOPEE_PARTNER_KEY=
SHOPEE_REDIRECT_URI=
SHOPEE_REDIRECT_ORIGIN=
SHOPEE_API_BASE="https://partner.shopeemobile.com"

# =======================
# Bling
# =======================
BLING_CLIENT_ID=
BLING_CLIENT_SECRET=
BLING_REDIRECT_URI=
BLING_TIPO_INTEGRACAO="API"
BLING_ID_LOJA=

# =======================
# Cron Jobs
# =======================
# Chave secreta para endpoints de cron (gere uma nova com: openssl rand -hex 32)
CRON_SECRET=
